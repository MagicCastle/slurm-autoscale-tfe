[tool.poetry]
name = "elastic-slurm-tf-cloud"
version = "0.1.0"
description = ""
authors = ["Félix-Antoine Fortin <felix-antoine.fortin@calculquebec.ca>"]
license = "MIT"

[tool.poetry.dependencies]
python = ">=3.6.2"
python-hostlist = "^1.21"
requests = ">=2"

[tool.poetry.dev-dependencies]
black = "^22.6.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
slurm_suspend = "elastic_slurm_tf_cloud:main"
slurm_resume = "elastic_slurm_tf_cloud:main"
